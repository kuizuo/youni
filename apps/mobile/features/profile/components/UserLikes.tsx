import { YStack, Text } from "@/ui";

interface Props {
  userId: string
}

export const UserLikes = ({ userId }: Props) => {
  return (
    <YStack flex={1} backgroundColor={'$background'} >
      <Text>TODO</Text>
    </YStack>
  )
}