{
	"name": "@youni/infra",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "alchemy dev --stage local",
		"deploy": "NODE_ENV=production alchemy deploy --stage production",
		"destroy": "alchemy destroy --stage production"
	},
	"dependencies": {
		"dotenv": "catalog:"
	},
	"devDependencies": {
		"@youni/config": "workspace:*",
		"alchemy": "catalog:",
		"typescript": "^6"
	}
}
